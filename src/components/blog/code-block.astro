---
import { CodeBlock as CodeBlockInternal, type CodeBlockProps } from '@/components/blog/code-block';
import { type Props as $ } from 'astro-portabletext/types';

type Props = $<CodeBlockProps>;

const { node: props } = Astro.props;
---

<CodeBlockInternal
  client:visible
  {...props}
/>
