---
import { type Props as $, type Mark } from 'astro-portabletext/types';

type Props = $<Mark<{ href: string }>>;

const { node } = Astro.props;
---

<a
  href={node.markDef.href}
  target="_blank"
  rel="noreferrer"
>
  <slot />
</a>
