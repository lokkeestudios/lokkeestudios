---
import { Image as ImageInternal, type ImageProps } from '@/components/blog/image';
import { type Props as $ } from 'astro-portabletext/types';

type Props = $<ImageProps>;

const { node: props } = Astro.props;

export { type Props };
---

<ImageInternal
  client:visible
  {...props}
/>
